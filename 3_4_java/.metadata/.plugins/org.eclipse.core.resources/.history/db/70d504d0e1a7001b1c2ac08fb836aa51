// dataTypeJS.js

// JS 데이터 - String

let str2 = 'Hello World!'
console.log( str2.slice(0, 3) )  // 상한값 포함, 하한값 미포함 (n-1)
console.log( str2.slice(6, 11) ) 
console.log( str2.replace('World', 'doyeon') ) 
console.log( str2.replace(' World!', '') )  // 제거 역할도 됨

str2 = 'happydoyeon88@gmail.com'
console.log( str2.match(/.+(?=@)/) ) 


console.log('') 
const str = 'The quick brown fox jumps over the lazy dog.';
console.log(str.slice(31));
// expected output: "the lazy dog."
console.log(str.slice(4, 19));
// expected output: "quick brown fox"
console.log(str.slice(-4));
// expected output: "dog."
console.log(str.slice(-9, -5));
// expected output: "lazy"


//console.log(str.indexOf('doyeon')  !== -1)

//const result = 'Hello World!'.indexOf('World')
//console.log(result)
//console.log( 'Hello W   orld!'.length)

const paragraph = 'The quick brown fox jumps over the lazy dog. If the dog barked, was it really lazy?';

const searchTerm = 'dog';
const indexOfFirst = paragraph.indexOf(searchTerm);

console.log(`The index of the first "${searchTerm}" from the beginning is ${indexOfFirst}`);
// expected output: "The index of the first "dog" from the beginning is 40"

console.log(`The index of the 2nd "${searchTerm}" is ${paragraph.indexOf(searchTerm, (indexOfFirst + 1))}`);
